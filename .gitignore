# Python
.venv/
# GPT-SoVITS 런타임 전용 3.12 venv (in-process import용, 별도 분리)
.venv-voice/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# 비밀 — API 키는 .env에만 (커밋 금지)
.env

# 런타임 데이터 — 나비의 기억. 백업은 git이 아니라 파일 복사로
*.db
logs/

# GPT-SoVITS WebUI 런타임 상태 (마지막 로드 ckpt 경로 캐시, 자동 생성)
weight.json

# 임시 학습·정리 노트 (공식 문서는 루트의 0X_*.md)
notes/

# 음성 실험 입출력 (개인 녹음·합성 wav·받아쓰기 결과 — 커밋 대상 아님)
scripts/in/
scripts/out/
out/
voice_ref/

# fine-tune 데이터셋 (voice_ref 음원 파생물 — 대용량·저작권, 로컬 보관)
dataset/

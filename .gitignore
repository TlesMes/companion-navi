# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# 비밀 — API 키는 .env에만 (커밋 금지)
.env

# 런타임 데이터 — 나비의 기억. 백업은 git이 아니라 파일 복사로
*.db
logs/

# 임시 학습·정리 노트 (공식 문서는 루트의 0X_*.md)
notes/

# 음성 실험 산출물 (합성 wav·받아쓰기 결과 — 청취용 스크래치)
scripts/out/
